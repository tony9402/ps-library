#include<bits/stdc++.h>

using namespace std;

[[power]]

int main() {
    ios::sync_with_stdio(false);
    cin.tie(0);

    long long A, B, C; cin >> A >> B >> C;
    cout << power(A, B, C);

    return 0;
}